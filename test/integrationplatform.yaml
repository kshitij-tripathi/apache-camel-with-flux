apiVersion: camel.apache.org/v1
kind: IntegrationPlatform
metadata:
  name: camel-k
  namespace: camel-k
spec:
  build:
    publishStrategy: Spectrum
    registry:
      address: docker.io
      #organization:   # or leave empty for anonymous
      insecure: true
