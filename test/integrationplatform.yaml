apiVersion: camel.apache.org/v1
kind: IntegrationPlatform
metadata:
  name: camel-k
  namespace: camel-k
spec:
  build:
    registry:
      address: "docker.io"
      insecure: true
      skipPush: true  # this avoids pushing to remote registries
